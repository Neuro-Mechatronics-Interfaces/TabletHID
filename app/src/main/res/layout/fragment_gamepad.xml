<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_gaming"
    android:keepScreenOn="true">

    <!-- ── Status bar ───────────────────────────────────────── -->
    <LinearLayout
        android:id="@+id/statusBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="12dp"
        android:paddingVertical="4dp"
        android:background="@color/overlay_gaming_status_bar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <View
            android:id="@+id/ledStatus"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/shape_led"
            android:backgroundTint="@color/led_disconnected" />

        <TextView
            android:id="@+id/textConnStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Disconnected"
            android:textColor="@color/overlay_white_60"
            android:textSize="12sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnOrientationLock"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:contentDescription="@string/orientation_lock"
            app:icon="@drawable/ic_orientation_auto"
            app:iconSize="24dp"
            app:iconTint="@android:color/white" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSettings"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:contentDescription="Gamepad Settings"
            app:icon="@drawable/ic_settings"
            app:iconSize="28dp"
            app:iconTint="@android:color/white" />

    </LinearLayout>

    <!-- ── Top-left: LT / LB ─────────────────────────────────── -->
    <Button
        android:id="@+id/btnLt"
        android:layout_width="90dp"
        android:layout_height="44dp"
        android:text="LT"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp" />

    <Button
        android:id="@+id/btnLb"
        android:layout_width="90dp"
        android:layout_height="44dp"
        android:text="LB"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnLt"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp" />

    <!-- ── Top-right: RT / RB ────────────────────────────────── -->
    <Button
        android:id="@+id/btnRt"
        android:layout_width="90dp"
        android:layout_height="44dp"
        android:text="RT"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="4dp" />

    <Button
        android:id="@+id/btnRb"
        android:layout_width="90dp"
        android:layout_height="44dp"
        android:text="RB"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnRt"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="4dp" />

    <!-- ── Center-top: Back / Start (packed chain, centered) ─── -->
    <Button
        android:id="@+id/btnBack"
        android:layout_width="72dp"
        android:layout_height="40dp"
        android:text="Back"
        android:textSize="11sp"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:autoSizeTextType="uniform"
        android:autoSizeMinTextSize="7sp"
        android:autoSizeMaxTextSize="11sp"
        app:layout_constraintEnd_toStartOf="@id/btnStart"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp" />

    <Button
        android:id="@+id/btnStart"
        android:layout_width="72dp"
        android:layout_height="40dp"
        android:text="Start"
        android:textSize="11sp"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:autoSizeTextType="uniform"
        android:autoSizeMinTextSize="7sp"
        android:autoSizeMaxTextSize="11sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/btnBack"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        android:layout_marginTop="8dp"
        android:layout_marginStart="8dp" />

    <!-- ── D-Pad (left side, vertical center) ───────────────── -->
    <Button
        android:id="@+id/dpadUp"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="▲"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintBottom_toTopOf="@id/dpadLeft"
        app:layout_constraintStart_toStartOf="@id/dpadLeft"
        app:layout_constraintEnd_toEndOf="@id/dpadLeft" />

    <Button
        android:id="@+id/dpadLeft"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="◀"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintBottom_toTopOf="@id/leftJoystick"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginBottom="4dp" />

    <Button
        android:id="@+id/dpadRight"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="▶"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintTop_toTopOf="@id/dpadLeft"
        app:layout_constraintStart_toEndOf="@id/dpadLeft"
        android:layout_marginStart="4dp" />

    <Button
        android:id="@+id/dpadDown"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="▼"
        android:textColor="@color/white"
        android:backgroundTint="@color/overlay_white_20"
        app:layout_constraintTop_toBottomOf="@id/dpadLeft"
        app:layout_constraintStart_toStartOf="@id/dpadLeft"
        app:layout_constraintEnd_toEndOf="@id/dpadLeft"
        android:layout_marginTop="4dp" />

    <!-- ── Left joystick ─────────────────────────────────────── -->
    <com.tablet.hid.ui.gamepad.JoystickView
        android:id="@+id/leftJoystick"
        android:layout_width="160dp"
        android:layout_height="160dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="24dp"
        android:layout_marginBottom="24dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSingleJoystickSide"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="52dp"
        android:layout_height="44dp"
        android:minWidth="0dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:text="L"
        android:textColor="@color/white"
        android:visibility="gone"
        app:strokeColor="@color/overlay_white_60"
        app:layout_constraintBottom_toBottomOf="@id/leftJoystick"
        app:layout_constraintStart_toEndOf="@id/leftJoystick"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="8dp" />

    <!-- ── Right joystick ────────────────────────────────────── -->
    <com.tablet.hid.ui.gamepad.JoystickView
        android:id="@+id/rightJoystick"
        android:layout_width="160dp"
        android:layout_height="160dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp" />

    <!-- ── ABXY cluster (right side, vertical center) ─────────
         Y (top), X (left), B (right), A (bottom)              -->
    <Button
        android:id="@+id/btnY"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="Y"
        android:textColor="@color/white"
        android:backgroundTint="@color/btn_gamepad_y"
        app:layout_constraintBottom_toTopOf="@id/btnX"
        app:layout_constraintEnd_toEndOf="@id/btnX"
        app:layout_constraintStart_toStartOf="@id/btnX" />

    <Button
        android:id="@+id/btnX"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="X"
        android:textColor="@color/white"
        android:backgroundTint="@color/btn_gamepad_x"
        app:layout_constraintBottom_toTopOf="@id/rightJoystick"
        app:layout_constraintEnd_toStartOf="@id/btnB"
        android:layout_marginBottom="4dp"
        android:layout_marginEnd="4dp" />

    <Button
        android:id="@+id/btnB"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="B"
        android:textColor="@color/white"
        android:backgroundTint="@color/btn_gamepad_b"
        app:layout_constraintTop_toTopOf="@id/btnX"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="20dp" />

    <Button
        android:id="@+id/btnA"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="A"
        android:textColor="@color/white"
        android:backgroundTint="@color/btn_gamepad_a"
        app:layout_constraintTop_toBottomOf="@id/btnX"
        app:layout_constraintStart_toStartOf="@id/btnX"
        app:layout_constraintEnd_toEndOf="@id/btnX"
        android:layout_marginTop="4dp" />

    <!-- Full-canvas overlay for freely positionable macro buttons -->
    <FrameLayout
        android:id="@+id/macroOverlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:clickable="false"
        android:focusable="false"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/editModeBanner"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/overlay_gaming_panel"
        android:paddingStart="12dp"
        android:paddingEnd="8dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:gravity="center_vertical"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/statusBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Edit — drag to move · pinch to resize · tap for precise controls"
            android:textColor="@color/text_gaming_info"
            android:textSize="11sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnEditDone"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:minHeight="36dp"
            android:text="Done" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
